apiVersion: v1
kind: Service

metadata:
  name: orbitalnet-frontend-service

spec:
  selector:
    app: orbitalnet-frontend

  ports:
    - protocol: TCP
      port: 5173
      targetPort: 5173

  type: NodePort