apiVersion: v1
kind: Service

metadata:
  name: orbitalnet-backend-service

spec:
  selector:
    app: orbitalnet-backend

  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000

  type: ClusterIP